<Window x:Class="Gac4EasyDeploy.GacManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <StackPanel>
        <Button Command="{Binding Path=OpenFusionLog}" >Open FusionLog</Button>
        
        <TextBox TextChanged="TextBox_TextChanged">fdsa</TextBox>
        <ListView ItemsSource="{x:Null}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="140" Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Width="140" Header="Phone Number" DisplayMemberBinding="{Binding Phone}"/>
                    <GridViewColumn Width="140" Header="Country" DisplayMemberBinding="{Binding Country}" />
                    <GridViewColumn Width="140" Header="Total" DisplayMemberBinding="{Binding Total}" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Window>
